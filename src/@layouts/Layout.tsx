import React, { FC } from "react";
interface Props {
  children?: any;
}

const Layout: FC<Props> = ({ children }) => {
  return (
    <>
      {/* Header  */} 
      {/* children */}
            {/* Footer */}

    </>
  );
};

export default Layout;
